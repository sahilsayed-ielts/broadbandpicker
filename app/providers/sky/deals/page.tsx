import PrioritySeoPage, { priorityMetadata } from '@/components/PrioritySeoPage'
export const metadata = priorityMetadata('skyDeals')
export default function Page() { return <PrioritySeoPage pageKey="skyDeals" /> }
